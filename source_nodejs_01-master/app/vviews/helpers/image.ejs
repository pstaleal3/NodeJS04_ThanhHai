<% 
    imageHelper = (folder, imageName, alt = "Image",width="100",className = "img-responsive img-avatar") => {

        if(imageName == null){ 
            imageName = 'no-avatar.png';
        }
        return `<img class="${className}" src="${folder + imageName}" alt="${alt}" width=${width} />`;
    }
%>



